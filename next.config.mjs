/** @type {import('next').NextConfig} */
const nextConfig = {

  //output: "export",
  distDir: 'dist',
  basePath: "/nextjs-github-pages",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
